@model IEnumerable<AssetManagement.Models.Version>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VersionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VersionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Version_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Version_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Version_ID })
        </td>
    </tr>
}

</table>
