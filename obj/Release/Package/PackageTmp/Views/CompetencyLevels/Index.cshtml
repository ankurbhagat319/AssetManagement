@model IEnumerable<AssetManagement.Models.CompetencyLevel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Level_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InsertedBy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InsterDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Level_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InsertedBy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InsterDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Level_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Level_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Level_ID })
        </td>
    </tr>
}

</table>
